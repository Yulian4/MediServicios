<odoo>
  <template id="preparacionq_form" name="Formulario de Preparación Quirúrgica">
    <t t-call="website.layout">
      <div class="container mt32">
        <h2>Crear Nueva Preparación Quirúrgica</h2>
        <form action="/medi_serv/preparacionq/create" method="post">
          <div class="form-group">
            <label for="paciente_id">Paciente</label>
            <select name="paciente_id" class="form-control" required="required">
              <t t-foreach="pacientes" t-as="paciente">
                <option t-att-value="paciente.id"><t t-esc="paciente.name"/></option>
              </t>
            </select>
          </div>
          <div class="form-group">
            <label for="descripcion">Descripción</label>
            <textarea name="descripcion" class="form-control" required="required"></textarea>
          </div>
          <div class="form-group">
            <label for="fecha_preparacion">Fecha de Preparación</label>
            <input type="date" name="fecha_preparacion" class="form-control" required="required"/>
          </div>
          <button type="submit" class="btn btn-primary">Guardar</button>
        </form>
      </div>
    </t>
  </template>
</odoo>
